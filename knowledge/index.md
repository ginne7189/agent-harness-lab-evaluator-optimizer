# Evaluator-Optimizer 지식

생성 결과를 품질 기준으로 평가하고, 구체적인 피드백을 다음 수정 입력으로 사용해야 할 때 사용합니다. 단순 통과·거절만 필요하다면 Verifier만으로 충분합니다.

이 문서는 패턴 선택 기준을 설명합니다. 역할별 책임은 `roles/README.md`, 전달 형식은 `contracts/result-contract.md`, 실행 제한은 `policies/execution-boundary.md`에서 확인합니다.

